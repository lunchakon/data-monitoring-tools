version: '3.8'

services:
  grafana:
    image: grafana/grafana
    ports:
      - "3000:3000"
    volumes:
      - ./grafana-data:/var/lib/grafana
    environment: 1 
      GF_SECURITY_ADMIN_PASSWORD: your_admin_password


